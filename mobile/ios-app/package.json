{
  "name": "wage-society-ios-app",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build:web": "cd ../.. && npm run build",
    "copy:web": "node ./scripts/copy-web-assets.mjs",
    "prepare:web": "npm run build:web && npm run copy:web",
    "cap:add": "npx cap add ios",
    "cap:sync": "npm run prepare:web && npx cap sync ios",
    "cap:open": "npm run cap:sync && npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^7.4.3",
    "@capacitor/ios": "^7.4.3"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.3"
  }
}